{% extends 'base_admin.html.twig' %}

{% block title %}users{% endblock %}

{% block body %}
<div class="m-auto w-1/2">
    <h1 class="text-3xl font-bold">Liste des utilisateurs</h1>
    <div class="m-auto mb-3 flex justify-end">
        <a class="btn btn-primary btn-sm" href="{{path('app_register')}}">+</a>
    </div>
    <table class="table">
        <thead>
            <tr class="text-xl font-bold">
                <th>Email</th>
                <th>Rôle</th>
                <th class="text-right">Actions</th>
            </tr>
        <tbody>
            {% for user in users %}
            <tr class="hover:bg-gray-100">
                <td>{{user.email}}</td>
                <td>{{user.roles[0]}}</td>
                <td>
                    <div class="flex gap-5 justify-end">
                        <a href><i class="fa-solid fa-edit text-green-500"></i></a>
                        <a href=""><i class="fa-solid fa-trash text-red-500"></i></a>
                    </div>
                </td>
            </tr>
            {% endfor %}
        </tbody>
        </thead>
    </table>
</div>
{% endblock %}