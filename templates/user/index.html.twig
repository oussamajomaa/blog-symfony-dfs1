{% extends 'base.html.twig' %}

{% block title %}users{% endblock %}

{% block body %}
<h1 class="text-center text-3xl font-bold">Liste des utilisateurs</h1>
<table class="table w-1/2 m-auto my-10">
    <thead>
    <tbody>
        <tr>
            <th>Email</th>
            <th>Rôle</th>
            <th>Actions</th>
        </tr>
        {% for user in users %}
        <tr class="hover:bg-gray-100">
            <td>{{user.email}}</td>
            <td>{{user.roles[0]}}</td>
            <td>
                <div class="flex gap-5">
                    <a href><i class="fa-solid fa-edit text-green-500"></i></a>
                    <a href=""><i class="fa-solid fa-trash text-red-500"></i></a>
                </div>
            </td>
        </tr>
        {% endfor %}
    </tbody>
    </thead>
</table>
{% endblock %}