{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tag.name }}</td>
            </tr>
        </tbody>
    </table>
<div class="flex justify-end gap-6 items-center">

    <a href="{{ path('app_tag_index') }}" class=" text-blue-400">Back</a>

    <a href="{{ path('app_tag_edit', {'id': tag.id}) }}" class="btn btn-warning  btn-sm my-3">edit</a>
</div>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
